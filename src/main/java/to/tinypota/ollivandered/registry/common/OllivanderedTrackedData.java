package to.tinypota.ollivandered.registry.common;

public class OllivanderedTrackedData {
//	public static final TrackedData<OptionalInt> INFLATED_HEAD = DataTracker.registerData(PlayerEntity.class, TrackedDataHandlerRegistry.OPTIONAL_INT);
//	public static final TrackedData<Boolean> IS_INVISIBLE = DataTracker.registerData(PlayerEntity.class, TrackedDataHandlerRegistry.BOOLEAN);

    public static void init() {
    }
}
